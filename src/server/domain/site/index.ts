// =========================================================
// 놓을 위치: src/server/domain/site/index.ts
//
// 검색엔진에 알리는 값들. (사용자 요청 2026-08-15 —
// "네이버 검색하면 나오게 하고싶어")
//
// ★ 주소·소개문을 **한 곳에** 둡니다. robots.txt · sitemap.xml ·
//   페이지 메타 정보가 같은 값을 봅니다. 흩어져 있으면 도메인을
//   바꿀 때 한 곳이 옛 주소로 남고, 그러면 검색엔진이 두 사이트로
//   봅니다 — 그게 SEO 에서 제일 흔한 자해입니다.
//
// ★ 지어낸 말을 안 씁니다. 여기 적힌 것은 홈페이지에 실제로 적혀
//   있는 것들뿐입니다 ([[ds-flow-homepage]] 의 원칙 그대로).
//   검색 결과에 뜨는 문장이라 더더욱 그렇습니다 — 보고 들어온
//   사람이 화면에서 다른 말을 보면 그대로 나갑니다.
// =========================================================

/** 정본 주소. www 는 여기로 넘어옵니다 */
export const SITE_URL = 'https://denflow.kr';

/** 상호. 검색·서류·화면이 모두 이 이름 하나를 씁니다 */
export const SITE_NAME = '덴플로우 디지털 기공소';

/**
 * ★ 붙여 쓴 이름도 함께 알립니다.
 *   사람들은 '덴플로우디지털기공소' 처럼 붙여 칩니다. 한글 검색은
 *   띄어쓰기를 웬만큼 눙쳐 주지만, 상호는 확실히 못 박아 두는 편이
 *   낫습니다 (구조화 데이터의 alternateName).
 */
export const SITE_NAME_TIGHT = '덴플로우디지털기공소';

/**
 * 무엇을 하는 곳인가. 홈페이지 첫 줄·설명·미리보기가 씁니다.
 *
 * ★ 상호와 겹칩니다 — `덴플로우 디지털 기공소` 의 뒷부분입니다
 *   (2026-08-18). 겹치는 것이 흠이 아니라, 이름과 하는 일이 같은
 *   말이라 서로를 받쳐 줍니다. 전에는 `모델리스 전문 기공소` 였습니다.
 */
export const SITE_TAGLINE = '디지털 기공소';

/**
 * 검색 결과에 뜨는 이름 — **상호와 같습니다** (사용자 요청 2026-08-18).
 *
 * ★ 반나절 동안 이름이 둘이었습니다.
 *   서류는 `덴플로우 치과기공소`, 검색은 `덴플로우 디지털 기공소` 로
 *   갈라 뒀다가 하나로 합쳤습니다. 이름이 둘이면 구조화 데이터로
 *   묶어 줘야 하고, 명함·세금계산서·플레이스가 늘 때마다 "어느
 *   쪽이었지" 를 다시 따져야 합니다. **하나로 두는 편이 낫습니다.**
 *
 * ★ 설명은 안 붙입니다. 한때 `DS 덴탈랩 · 모델리스 전문 기공소` 였는데
 *   이름 뒤에 설명이 따라붙는 것이 지저분하다는 지적을 받았습니다.
 *   무엇을 하는 곳인가는 아래 설명 줄이 맡습니다.
 */
export const SITE_TITLE = SITE_NAME;

/**
 * 대표 로고 (정사각 PNG).
 *
 * ★ 검색 결과에 로고가 뜨려면 **주소가 안 변해야** 합니다.
 *   그래서 빌드마다 이름이 바뀌는 곳(_next)이 아니라 `public/` 에 둡니다.
 *   그림을 바꿀 때는 `node scripts/make-icons.mjs` 로 다시 만듭니다 —
 *   파비콘·아이폰 아이콘과 같은 마크에서 함께 나옵니다.
 */
export const SITE_LOGO = '/logo.png';
export const SITE_LOGO_SIZE = 512;

/** 홈페이지에 적혀 있는 대표 번호와 같은 값입니다 */
export const SITE_TEL = '010-3365-3145';

/**
 * 검색 결과의 설명 줄 — **첫 화면 문구 그대로** (사용자 요청 2026-08-18).
 *
 * ★ 검색에서 보이는 글과 눌러서 보이는 글이 같아야 합니다.
 *   검색 결과를 보고 들어온 사람이 첫 화면에서 다른 말을 보면, 자기가
 *   찾던 곳이 맞는지 한 번 의심하고 그 사이에 나갑니다. 그래서
 *   홈페이지 큰 글씨(h1)와 그 아래 소개를 그대로 옮겼습니다.
 *
 * ★★ **검색엔진이 이 글을 안 쓸 수도 있습니다.**
 *   네이버는 본문에서 알아서 문장을 뽑아 쓰는 일이 잦습니다 — 실제로
 *   `/signup` 이 그렇게 걸렸고, 우리가 적어 둔 설명 대신 가입 안내
 *   두 줄이 그대로 검색 결과에 나왔습니다.
 *   그래서 **화면의 글과 이 글을 같게** 두는 것이 유일하게 확실한
 *   방법입니다. 어느 쪽을 골라 쓰든 같은 말이 나옵니다.
 *
 * ★ 네이버·구글 모두 80~120자 어름만 보여 주고 뒤는 자릅니다.
 *
 * ★ '모델리스' 가 이 줄에서 빠졌습니다.
 *   화면 본문의 '왜 모델리스인가' 절이 그 말을 갖고 있습니다 —
 *   검색엔진은 본문도 읽습니다. 다만 설명 줄보다 약한 신호입니다.
 */
export const SITE_DESCRIPTION =
  `${SITE_TAGLINE}. 모델은 생략하고, 과정은 간결하게 — 스캔에서 바로 보철로. ` +
  '모델 제작의 번거로움 없이 스캔 데이터에서 최종 보철까지, 하나의 디지털 프로세스로.';

/**
 * ★ 네이버 서치어드바이저 소유확인 코드.
 *
 *   비어 있으면 아무것도 안 붙습니다. 채우는 법:
 *     1) searchadvisor.naver.com → 웹마스터도구 → 사이트 등록
 *     2) `https://denflow.kr` 입력
 *     3) 소유확인 방법에서 **HTML 태그** 선택
 *     4) `<meta name="naver-site-verification" content="여기">` 의
 *        content 값만 아래에 붙여 넣기
 *
 *   ★ 파일 업로드 방식이 아니라 **HTML 태그** 방식을 고르세요.
 *     파일 방식은 `public/` 에 파일을 하나 더 두는 것인데, 그 파일이
 *     왜 있는지 반년 뒤에 아무도 모릅니다. 이 값은 여기 주석과 함께
 *     남습니다.
 */
export const NAVER_VERIFICATION = '7930a62dbf7d7dbf83d8acea3908c28b5373ebbb';

/** 구글 서치콘솔 소유확인 코드 (같은 방식) */
export const GOOGLE_VERIFICATION = 'CwtsIU8bxVDUbDuSQR-z7nWfHIP8dawoxsJ3bHdWwbc';

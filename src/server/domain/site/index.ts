// =========================================================
// 놓을 위치: src/server/domain/site/index.ts
//
// 검색엔진에 알리는 값들. (사용자 요청 2026-08-15 —
// "네이버 검색하면 나오게 하고싶어")
//
// ★ 주소·소개문을 **한 곳에** 둡니다. robots.txt · sitemap.xml ·
//   페이지 메타 정보가 같은 값을 봅니다. 흩어져 있으면 도메인을
//   바꿀 때 한 곳이 옛 주소로 남고, 그러면 검색엔진이 두 사이트로
//   봅니다 — 그게 SEO 에서 제일 흔한 자해입니다.
//
// ★ 지어낸 말을 안 씁니다. 여기 적힌 것은 홈페이지에 실제로 적혀
//   있는 것들뿐입니다 ([[ds-flow-homepage]] 의 원칙 그대로).
//   검색 결과에 뜨는 문장이라 더더욱 그렇습니다 — 보고 들어온
//   사람이 화면에서 다른 말을 보면 그대로 나갑니다.
// =========================================================

/** 정본 주소. www 는 여기로 넘어옵니다 */
export const SITE_URL = 'https://denflow.kr';

/** 검색 결과의 제목 줄 */
export const SITE_TITLE = 'DS 덴탈랩 · 모델리스 전문 기공소';

/**
 * 검색 결과의 설명 줄.
 *
 * ★ 네이버·구글 모두 **80~120자** 어름만 보여 주고 뒤는 자릅니다.
 *   그래서 제일 중요한 말을 앞에 둡니다 — 무엇을 하는 곳인가.
 */
export const SITE_DESCRIPTION =
  '모델을 뜨지 않고 구강 스캔 데이터에서 바로 설계하고 밀링합니다. ' +
  '지르코니아 크라운·인레이·임플란트 보철. 수가표를 먼저 받아 보세요.';

/**
 * ★ 네이버 서치어드바이저 소유확인 코드.
 *
 *   비어 있으면 아무것도 안 붙습니다. 채우는 법:
 *     1) searchadvisor.naver.com → 웹마스터도구 → 사이트 등록
 *     2) `https://denflow.kr` 입력
 *     3) 소유확인 방법에서 **HTML 태그** 선택
 *     4) `<meta name="naver-site-verification" content="여기">` 의
 *        content 값만 아래에 붙여 넣기
 *
 *   ★ 파일 업로드 방식이 아니라 **HTML 태그** 방식을 고르세요.
 *     파일 방식은 `public/` 에 파일을 하나 더 두는 것인데, 그 파일이
 *     왜 있는지 반년 뒤에 아무도 모릅니다. 이 값은 여기 주석과 함께
 *     남습니다.
 */
export const NAVER_VERIFICATION = '7930a62dbf7d7dbf83d8acea3908c28b5373ebbb';

/** 구글 서치콘솔 소유확인 코드 (같은 방식) */
export const GOOGLE_VERIFICATION = '';

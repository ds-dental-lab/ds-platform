// =========================================================
// 놓을 위치: src/app/(auth)/signup/layout.tsx
//
// 가입 화면의 제목·설명만 담습니다. (2026-08-18)
//
// ★ 화면이 'use client' 라 metadata 를 못 답니다. 그래서 얇은
//   레이아웃 하나를 둡니다 — 그리는 것은 아무것도 없습니다.
//
// ★★ 이걸 만든 이유 — **가입 화면이 대표 결과로 잡혔습니다.**
//   홈페이지와 제목·설명이 **글자 하나까지 같았습니다**(둘 다 뿌리
//   레이아웃 값을 그대로 물려받았습니다). 검색엔진은 같은 글을 단
//   둘 중 하나만 고르는데, 네이버가 이쪽을 골랐습니다.
//   실제로 '덴플로우' 검색 결과에 `denflow.kr › signup` 이 떴고,
//   설명은 우리가 적어 둔 것이 아니라 화면 본문의 안내 두 줄이었습니다.
//
//   화면마다 제 이름과 제 설명을 달아 두면 이런 일이 없습니다.
// =========================================================

import type { Metadata } from 'next';

export const metadata: Metadata = {
  // 뿌리 레이아웃의 틀이 붙어 "거래 신청 · 덴플로우 디지털 기공소" 가 됩니다
  title: '거래 신청',
  description:
    '치과·기공소가 덴플로우와 거래를 시작하는 곳입니다. ' +
    '기관 정보를 남기시면 확인 후 계정을 열어 드립니다.',
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}

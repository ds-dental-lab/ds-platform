// =========================================================
// 놓을 위치: src/app/(auth)/layout.tsx
//
// 로그인·가입·비밀번호 화면 셋에 **검색 색인 금지**를 겁니다.
// (사용자 지적 2026-09-04 — "덴플로우 덴탈랩 검색하니까 DS 덴탈랩이
//  검색이 되네 없애줘")
//
// ★★ 무슨 일이었나. 상호로 검색했는데 홈페이지가 아니라 **가입 페이지**가
//   첫 결과로 떴습니다. 옛 상호(DS 덴탈랩)가 든 제목을 네이버가 캐시한
//   채로요. 8월 18일에 가입 화면 제목을 홈페이지와 갈라 줬는데 그것으로는
//   부족했습니다 — 검색엔진은 여전히 이쪽을 골랐습니다.
//
// ★ 이 세 화면은 검색에서 들어올 이유가 없습니다. 홈페이지에서 '로그인'
//   '거래 신청' 을 누르면 가고, 비밀번호 화면은 메일 링크로만 옵니다.
//   검색이 잡아야 할 것은 **간판 하나**, 홈페이지입니다.
//
// ★ `follow: true` — 이 화면 안의 링크(홈페이지·약관·처리방침)는 따라가도
//   됩니다. 막는 건 이 화면 **자체**가 결과에 뜨는 것뿐입니다.
//
// ★ 그리는 것은 없습니다. 자식 레이아웃(signup·login)의 제목이 그대로
//   살고, robots 만 여기서 한 번에 붙습니다.
//
// ★ 이미 잡힌 결과는 코드로 못 지웁니다. 네이버 서치어드바이저에서
//   `denflow.kr/signup` 을 **수집 요청**해야 새 태그를 읽고 내려갑니다
//   (domain/site 주석에 들어가는 길이 있습니다).
// =========================================================

import type { Metadata } from 'next';

export const metadata: Metadata = {
  robots: { index: false, follow: true },
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}

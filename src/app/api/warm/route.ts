// =========================================================
// 놓을 위치: src/app/api/warm/route.ts
//
// 서버를 깨워 두는 곳. (사용자 지적 2026-09-06 — "전체적으로 반응속도가 느려")
//
// ★ 느림의 정체는 코드가 아니라 **콜드 스타트** 였습니다.
//   7분 쉬었다가 첫 화면을 열면 3.7초, 이어지는 탭은 0.3초. Vercel 이
//   아무도 안 쓰는 동안 프로세스를 내리고, 다음 사람이 오면 19MB 함수를
//   새로 띄웁니다. 거래처가 적은 지금은 거의 매번 첫 클릭이 여기 걸립니다.
//
// ★ 이 주소를 **몇 분마다 한 번** 밖에서 불러 주면 프로세스가 안 내려갑니다.
//   Hobby 플랜은 Vercel cron 이 하루 한 번뿐이라, 외부 핑 서비스
//   (cron-job.org 같은 무료 서비스)로 4분 간격을 겁니다.
//   페이지 함수들은 한 덩어리로 묶여 배포되므로 이 가벼운 경로 하나만
//   깨워도 화면 함수가 같이 깨어 있습니다.
//
// ★ DB 는 안 건드립니다. 깨우는 게 목적이라 아무것도 안 읽는 것이 맞고,
//   로그인도 필요 없습니다 — 내용이 없으니 새어 나갈 것도 없습니다.
// =========================================================

export const dynamic = 'force-dynamic';

export function GET() {
  return new Response('ok', {
    headers: { 'cache-control': 'no-store' },
  });
}

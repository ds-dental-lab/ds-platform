-- =========================================================
-- DS Flow — 주문유형에 '아날로그' 추가
-- 파일 위치: supabase/migrations/<타임스탬프>_add_analog_order_type.sql
-- 기준: 시안 주문등록 화면
--
-- 화면에서 고를 수 있는 주문유형은 모델리스 · 아날로그 둘입니다.
-- with_model · model_only 는 enum 에 남겨 둡니다 —
-- 이미 그 값으로 저장된 주문이 있으면 지울 수 없습니다.
--
-- ★ 명세서 §4.3 의 '아날로그 이슈'와는 다른 것입니다.
--   이슈는 order_issues 에 있고, 여기는 주문의 종류입니다.
-- =========================================================

alter type order_type add value if not exists 'analog';

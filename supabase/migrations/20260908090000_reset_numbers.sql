-- =========================================================
-- 번호를 처음부터 다시 셉니다. (사용자 결정 2026-09-08 — 시험 자료를
-- 전부 지운 뒤 "다시 세게 해줘")
--
-- ★ 주문번호(ORD-YYMMDD-NNN)는 날짜별 카운터(order_no_counters)라
--   줄을 지우면 그날 001 부터입니다 — 이미 REST 로 지웠습니다.
-- ★ 청구서(INV-)·credit note(CN-) 번호는 시퀀스라 여기서 되돌립니다.
--   지금까지 나간 INV-26000011 은 시험 자료와 함께 지워졌으므로
--   같은 번호가 두 문서에 붙을 일이 없습니다.
-- ★ 실제 거래가 시작된 뒤에는 **절대 다시 돌리지 않습니다** —
--   번호는 한 번 나가면 끝입니다.
-- =========================================================

alter sequence invoice_no_seq restart with 1;
alter sequence credit_no_seq restart with 1;

# 운영 안내

만들어 둔 것을 **어떻게 굴리는가**. 배포 절차는 `DEPLOY.md`,
시험 서버는 `STAGING.md` 에 있습니다.

---

## 1. 지금 제일 급한 것 셋

### ① 백업이 한 벌도 없습니다  ← 제일 위험

Supabase **무료 요금제는 자동 백업을 안 해 줍니다.** 실수로 지운 것도,
잘못 돌린 마이그레이션도 되돌릴 방법이 없습니다.

당장은 이걸로 막습니다:

```bash
npm run backup
```

표 52개를 `backup/<날짜>/` 로 통째로 받습니다.
★ **환자 실명이 들어 있는 폴더입니다.** git 에는 안 올라가게 막아 뒀지만,
메일·메신저로 옮기지 마세요. 외장 디스크나 잠긴 폴더에 두세요.

**언제 돌리나** — ① 마이그레이션을 올리기 **직전**
② 시험 자료를 정리하기 직전 ③ 그밖에 주 1회.

**진짜 답은 Supabase Pro** ($25/월). 하루 단위 자동 백업이 붙고,
**1주일 안 쓰면 프로젝트가 잠기는 것**도 없어집니다. 거래처를
받기 시작하면 그 전에 올리세요.

### ② 사업자등록 — 세 가지가 여기 묶여 있습니다

- 개인정보 처리방침 **시행** (지금은 초안 상태)
- 카카오 알림톡 **발송** (번호·대기열까지는 다 만들어 뒀습니다)
- 네이버 **플레이스** (치과가 기공소를 찾을 때 실제로 닿는 자리)

### ③ 운영 DB 에 시험 자료가 섞여 있습니다

주문 38건 중 **살아 있는 것은 1건**, 나머지는 지운 자국입니다.
치과 4곳 중 실제 거래처가 아닌 것도 있습니다.
진짜 거래처를 받기 **전에** 한 번 정리하세요 —
사용자탭 휴지통(주문·정산 기록이 없는 줄만 지워집니다)과
보관기간 화면의 파기가 그 도구입니다.

---

## 2. 정기적으로 볼 것

| 언제 | 무엇 | 어디서 |
|---|---|---|
| 매일 | 안 읽은 대화 | HOME 맨 위 주황 띠 |
| 매일 | 가입 신청 | 사이드바 `가입승인` 배지 |
| 매일 | 수가표 요청 | 사이드바 `수가표 요청` 배지 |
| 주 1회 | 백업 | `npm run backup` |
| 주 1회 | 재스캔·지연 건 | 주문목록 필터 |
| 매달 | 청구서 발행 | 정산관리 (기준일 지난 뒤) |
| 매달 | 리메이크 사유 | 통계 — 같은 원인이 반복되는지 |
| 분기 | 보관기간 파기 | 계정정보 → 보관기간 (사람이 눌러야 돕니다) |

★ **Supabase 는 1주일 아무 접속이 없으면 잠깁니다.** 무료 요금제인
동안은 주에 한 번은 열어 두세요 (백업 돌리면 그것도 접속입니다).

---

## 3. 사고가 났을 때

**화면이 안 뜬다** → Vercel 대시보드 → Deployments 에서 최근 배포 상태.
빨간 줄이면 그 줄을 눌러 로그를 보고, 급하면 **Instant Rollback** 으로
직전 배포로 되돌립니다.

**자료가 이상하다** → 먼저 `npm run backup` 을 돌려 **지금 상태를
받아 두세요.** 그다음에 손대야 합니다. 되돌릴 곳이 없으면 복구가 아니라
두 번째 사고가 됩니다.

**누가 무엇을 봤는지 알아야 한다** → 계정정보 → 열람 기록.

**비밀번호를 잊었다** → 로그인 화면의 '비밀번호 찾기'.
관리자도 남의 비밀번호를 볼 수 없습니다(그래야 맞습니다).

---

## 4. 코드를 고칠 때

```bash
npm test          # 도메인 규칙 — 여기가 기준선입니다
npx tsc --noEmit  # 타입
npm run build     # 빌드
git push          # 밀면 Vercel 이 알아서 배포합니다
```

DB 를 고쳤으면 마이그레이션도 함께:

```bash
npx supabase@latest db push --linked
```

★ **마이그레이션은 되돌리기가 어렵습니다.** 올리기 전에 백업.
★ 화면을 고쳤으면 **브라우저로 열어서** 확인하세요. 빌드가 통과했다는
것과 화면이 맞다는 것은 다른 이야기입니다.

---

## 5. 사람과 권한

- 계정은 **관리자가 직접 만듭니다** (사용자탭). 가입 신청은 승인제입니다
- 치과·기공소는 **자기 것만** 봅니다. 화면이 아니라 DB(RLS)가 막습니다
- 기공소는 **배정받은 주문만** 봅니다 — 그래서 환자 이름이 보여도 됩니다
- 기공소는 치과 판매가를, 치과는 기공원가를 **절대 못 봅니다**
- 금액이 나오는 화면은 **관리자만** 들어갑니다

---

## 6. 아직 안 켠 것

- **카카오 알림톡 발송** — 사업자등록 대기
- **청구서 자동 메일** — 지금은 사람이 보내고 '보냄' 을 눌러 두는 기록
- **홈페이지 사진** — 장비·작업물 (지금 미리보기 그림은 그린 것입니다)
- **검색 등록 마무리** — 네이버 사이트맵 제출 · 구글 색인 요청
